# Design Docs

This repository now keeps design material under `design/`.

## Docs

- `design/overview.md` — current architectural overview, canonical local truth, cloud role, and guardrails
- `design/enriched-snapshot-sync.md` — proposal for syncing installation-level enriched top snapshots to cloud before later event-sync work

## Notes

- `overview.md` captures the current system model and invariants
- proposal docs in `design/` are intentionally more change-oriented and can evolve independently
- follow-up event-level sync work is tracked separately in Issue #46
